import { WorkspacePlaceholder } from '@/components/workspace-placeholder';

export default function TerminalPage() {
  return (
    <WorkspacePlaceholder
      title="Terminal"
      description="Terminal workspace for command execution and diagnostics."
    />
  );
}
