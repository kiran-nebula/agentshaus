import { WorkspacePlaceholder } from '@/components/workspace-placeholder';

export default function BookmarksPage() {
  return (
    <WorkspacePlaceholder
      title="Bookmarks"
      description="Saved destinations and quick links for your workflow."
    />
  );
}
